import { ComponentStyleConfig } from "@chakra-ui/react";
import {
  sizes,
  solid,
  outline,
  solidSecondary,
  outlineSecondary,
} from "./Button";
export const Button: ComponentStyleConfig = {
  sizes,
  variants: {
    solid,
    outline,
    solidSecondary,
    outlineSecondary,
  },
};
