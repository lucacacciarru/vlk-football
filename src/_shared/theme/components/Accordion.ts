import { ComponentStyleConfig } from '@chakra-ui/react';

export const Accordion: ComponentStyleConfig = {
  baseStyle: {
    button: {
      _focus: {
        boxShadow: 'none',
      },
    },
  },
};
