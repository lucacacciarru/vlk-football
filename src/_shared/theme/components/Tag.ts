import { ComponentStyleConfig } from '@chakra-ui/react';

export const Tag: ComponentStyleConfig = {
  baseStyle: {
    closeButton: {
      _focus: {
        boxShadow: 'none',
      },
    },
  },
};
