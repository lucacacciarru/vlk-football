import { render } from "../../../_shared/testConfig/customRender";
import { ContainerColumn } from "./ContainerColumns";

describe("ContainerColumns component", () => {
  test("Should be rendered", () => {
    render(<ContainerColumn />);
  });
});
